# Wallet Evaluation Project

## Setup
...

## Assumptions
- Real authentication is out of scope for this evaluation (coordinated with the company); `AllowAny` is used as the default DRF permission, documented in `config/settings.py`.
